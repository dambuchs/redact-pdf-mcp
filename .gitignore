node_modules/
dist/
*.tsbuildinfo

# Never commit credentials. This package is mirrored to a PUBLIC repo and
# published from there, so a project-local .npmrc holding an npm token would
# be pushed with it.
.npmrc
.env
.env.*
