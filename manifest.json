{
  "manifest_version": "0.2",
  "name": "redact-pdf-mcp",
  "display_name": "Redact PDF AI",
  "version": "0.2.1",
  "description": "Permanently redact PII from PDFs and scans. EU/Swiss-hosted, never used to train AI models.",
  "long_description": "An MCP server that permanently redacts PII from PDFs, photos and screenshots. The sensitive text is deleted from the file rather than covered with a black rectangle, so it cannot be recovered by copy-paste, text extraction, or \"remove object\" in a PDF editor.\n\nMost MCP \"redaction\" servers scrub PII out of prompt text. This one takes a real document and returns a real redacted document.\n\nIncludes a keyless try_demo tool, so an agent can verify the server works before any API key is configured. Handles scanned documents via OCR across 100+ languages and detects 8 entity types. Documents are processed on Microsoft Azure in the EU and Switzerland, encrypted in transit and at rest, never used to train AI models, and the original is deleted after processing. The server itself is stateless and sends no telemetry.",
  "author": {
    "name": "Redact PDF AI",
    "email": "info@redact-pdf.ai",
    "url": "https://www.redact-pdf.ai"
  },
  "homepage": "https://www.redact-pdf.ai/developers",
  "documentation": "https://www.redact-pdf.ai/docs",
  "support": "https://github.com/dambuchs/redact-pdf-mcp/issues",
  "license": "MIT",
  "keywords": [
    "pdf",
    "redaction",
    "pii",
    "privacy",
    "gdpr",
    "hipaa",
    "ocr",
    "documents",
    "anonymization",
    "de-identification",
    "mcp"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/dambuchs/redact-pdf-mcp"
  },
  "server": {
    "type": "node",
    "entry_point": "dist/stdio.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/stdio.js"
      ],
      "env": {
        "REDACT_PDF_API_KEY": "${user_config.api_key}"
      }
    }
  },
  "user_config": {
    "api_key": {
      "type": "string",
      "title": "Redact PDF AI API key",
      "description": "Your API key from redact-pdf.ai. Optional: without one, the keyless try_demo tool still works (it redacts the first page of a real file) so you can see output before signing up. A free account gets its first document, up to 5 pages, redacted in full with no card.",
      "sensitive": true,
      "required": false
    }
  },
  "compatibility": {
    "runtimes": {
      "node": ">=18.0.0"
    }
  }
}
